<Window x:Class="StadiumManagementSystem.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Login}" Height="350" Width="450"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="{DynamicResource Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,30" TextAlignment="Center" Foreground="#1F4E78"/>
            
            <TextBlock Text="{DynamicResource Username}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username}" Margin="0,0,0,15" Padding="5"/>
            
            <TextBlock Text="{DynamicResource Password}" Margin="0,0,0,5"/>
            <PasswordBox Name="PasswordBox" Margin="0,0,0,5" Padding="5"/>
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,0,0,10" Height="20"/>
            
            <Button Content="{DynamicResource Login}" 
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=PasswordBox}"
                    Padding="10" Background="#1F4E78" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>

<UserControl x:Class="StadiumManagementSystem.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource Customers}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" Margin="0,0,10,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                    <DataGridTextColumn Header="Total Spent" Binding="{Binding TotalSpent, StringFormat=N0}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Effect="{StaticResource CardShadow}" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="{DynamicResource PrintStatement}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}"/>
                        <TextBlock Text="{Binding SelectedCustomer.Name}" FontSize="16" Margin="0,5,0,0"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding CustomerBookings}" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding BookingDate, StringFormat=d}" Width="80"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat=N0}" Width="80"/>
                            <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat=N0}" Width="80"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding PaymentStatus}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="2" Content="{DynamicResource PrintStatement}" Command="{Binding PrintStatementCommand}"
                            Margin="0,15,0,0" Padding="10" Background="{DynamicResource PrimaryColor}" Foreground="White" BorderThickness="0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

<UserControl x:Class="StadiumManagementSystem.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="{DynamicResource FlowDirection}">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- User List -->
        <Border Grid.Column="0" Background="White" Margin="0,0,10,0" Padding="15" CornerRadius="8" Effect="{StaticResource CardShadow}">
            <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource Username}" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="*"/>
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTemplateColumn Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" Background="#17A2B8" Foreground="White" BorderThickness="0" Padding="10,2" Margin="2"/>
                                    <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" Background="#DC3545" Foreground="White" BorderThickness="0" Padding="10,2" Margin="2"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- User Form -->
        <Border Grid.Column="1" Background="White" Margin="10,0,0,0" Padding="15" CornerRadius="8" Effect="{StaticResource CardShadow}">
            <StackPanel>
                <TextBlock Text="{DynamicResource UserManagement}" FontSize="20" FontWeight="Bold" Foreground="#1F4E78" Margin="0,0,0,15"/>

                <TextBlock Text="{DynamicResource Username}"/>
                <TextBox Text="{Binding SelectedUser.Username}" Margin="0,0,0,10" Padding="5"/>

                <TextBlock Text="{DynamicResource Password}"/>
                <TextBox Text="{Binding PasswordInput}" Margin="0,0,0,10" Padding="5"/>

                <TextBlock Text="Full Name"/>
                <TextBox Text="{Binding SelectedUser.FullName}" Margin="0,0,0,10" Padding="5"/>

                <TextBlock Text="Role"/>
                <ComboBox SelectedItem="{Binding SelectedUser.Role}" Margin="0,0,0,10">
                    <ComboBoxItem Content="Admin"/>
                    <ComboBoxItem Content="Staff"/>
                </ComboBox>

                <CheckBox Content="Is Active" IsChecked="{Binding SelectedUser.IsActive}" Margin="0,10,0,10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="{DynamicResource Save}" Command="{Binding SaveCommand}"
                            Padding="20,10" Background="#28A745" Foreground="White" FontWeight="Bold" Margin="5"/>
                    <Button Content="Clear" Command="{Binding ClearCommand}"
                            Padding="20,10" Background="Gray" Foreground="White" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="StadiumManagementSystem.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StadiumManagementSystem.Views"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource UserManagement}" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Users List -->
            <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" AutoGenerateColumns="False"
                      IsReadOnly="True" Background="White" BorderThickness="0" Margin="0,0,20,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource FullName}" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource Username}" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="{DynamicResource Role}" Binding="{Binding Role}" Width="*"/>
                    <DataGridCheckBoxColumn Header="{DynamicResource IsActive}" Binding="{Binding IsActive}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- User Form -->
            <Border Grid.Column="1" Background="White" Padding="20" CornerRadius="10" Effect="{StaticResource CardShadow}">
                <StackPanel>
                    <TextBlock Text="{DynamicResource UserDetails}" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                    <TextBlock Text="{DynamicResource FullName}"/>
                    <TextBox Text="{Binding FullName}" Margin="0,5,0,15" Padding="5"/>

                    <TextBlock Text="{DynamicResource Username}"/>
                    <TextBox Text="{Binding Username}" Margin="0,5,0,15" Padding="5"/>

                    <TextBlock Text="{DynamicResource Password}"/>
                    <TextBox Text="{Binding Password}" Margin="0,5,0,15" Padding="5"/>

                    <TextBlock Text="{DynamicResource Role}"/>
                    <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding Role}" Margin="0,5,0,15" Padding="5"/>

                    <CheckBox Content="{DynamicResource IsActive}" IsChecked="{Binding IsActive}" Margin="0,5,0,15"/>

                    <TextBlock Text="{DynamicResource Notes}"/>
                    <TextBox Text="{Binding Notes}" Height="80" Margin="0,5,0,20" Padding="5" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <UniformGrid Columns="2">
                        <Button Content="{DynamicResource Save}" Command="{Binding SaveUserCommand}" Margin="0,0,5,0" Padding="10"/>
                        <Button Content="{DynamicResource DeleteUser}" Command="{Binding DeleteUserCommand}" Margin="5,0,0,0" Padding="10" Foreground="Red"/>
                    </UniformGrid>

                    <Button Content="{DynamicResource Clear}" Command="{Binding ClearFieldsCommand}" Margin="0,10,0,0" Padding="5"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

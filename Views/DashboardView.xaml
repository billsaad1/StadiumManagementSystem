<UserControl x:Class="StadiumManagementSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="{DynamicResource Dashboard}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,20"/>

            <!-- Stats Cards -->
            <UniformGrid Columns="3" Margin="0,0,0,30">
                <Border Background="White" Margin="0,0,10,0" Padding="20" CornerRadius="8" Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource BookingsToday}" Foreground="Gray"/>
                        <TextBlock Text="{Binding TotalBookingsToday}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}"/>
                    </StackPanel>
                </Border>
                <Border Background="White" Margin="10,0,10,0" Padding="20" CornerRadius="8" Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource RevenueToday}" Foreground="Gray"/>
                        <TextBlock Text="{Binding TotalRevenueToday}" FontSize="24" FontWeight="Bold" Foreground="#28A745"/>
                    </StackPanel>
                </Border>
                <Border Background="White" Margin="10,0,0,0" Padding="20" CornerRadius="8" Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource AvailableSlots}" Foreground="Gray"/>
                        <TextBlock Text="{Binding AvailableSlotsToday}" FontSize="24" FontWeight="Bold" Foreground="#17A2B8"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Chart -->
                <Border Grid.Column="0" Background="White" Padding="20" CornerRadius="8" Height="400" Margin="0,0,10,0" Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource WeeklyRevenue}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                        <lvc:CartesianChart Series="{Binding RevenueSeries}" XAxes="{Binding XAxes}" Height="330"/>
                    </StackPanel>
                </Border>

                <!-- Reminders -->
                <Border Grid.Column="1" Background="White" Padding="20" CornerRadius="8" Height="400" Margin="10,0,0,0" Effect="{StaticResource CardShadow}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Reminders}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding UpcomingReminders}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#F0F2F5" BorderThickness="0,0,0,1" Padding="0,10">
                                        <Grid>
                                            <StackPanel>
                                                <TextBlock Text="{Binding CustomerName}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding TimeSlot}" FontSize="12" Foreground="Gray"/>
                                            </StackPanel>
                                            <Button Content="💬" 
                                                    Command="{Binding DataContext.SendReminderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    HorizontalAlignment="Right" VerticalAlignment="Center" Background="Transparent" BorderThickness="0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DockPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
